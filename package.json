{
  "name": "j-make",
  "version": "1.0.0",
  "description": "A client-side library that builds the DOM from a JSON array and populates elements via directory-based AJAX calls.",
  "main": "j-make.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/richardkentgates/j-make.git"
  },
  "author": "Richard Kent Gates",
  "license": "MIT",
  "devDependencies": {
    "jquery": "^3.7.1"
  },
  "scripts": {
    "serve": "npx serve ."
  }
}
